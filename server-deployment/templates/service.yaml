apiVersion: v1
kind: Service
metadata:
  name:  {{ .Release.Name }}-server
  labels:
    type:  {{ .Release.Name }}-server
spec:
  type: NodePort
  ports:
  - name: minecraft
    port: 25565
    protocol: TCP
  - name: rcon
    port: 25575
    protocol: TCP
  - name: extra
    port: 25567
  - name: ssh
    port: 22
    protocol: TCP
  selector:
    app:  {{ .Release.Name }}-server